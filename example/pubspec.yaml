name: full_context_example

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  full_context:
    path: ../

  go_router: ^17.0.1
  path: ^1.9.1
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0